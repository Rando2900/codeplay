<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="stylesheets/editarperfil.css">
    <title>Editar Perfil - Code-Play</title>
</head>
<body>
  <!-- Contenido principal de la página -->
  <div id="main-content">
    <header class="header">
      <nav class="nav">
        <a href="index.html">Inicio</a>
        <a href="comunidad.html">Comunidad</a>
        <a href="juegos.html">Juegos</a>
        <a href="formulario.html">Crear</a>
      </nav>
      <div class="logo">
        <a href="index.html"><img src="images/CodePlay_ejemplo_sin_fondo (1).png" alt="Logo del Proyecto" /></a>
      </div>
    </header>

<main class="profile-edit-container">
    <h1>Editar Perfil</h1>
    <form id="edit-profile-form" action="/users/updateProfile" method="POST">
        <div class="form-section">
            <label for="username">Usuario</label>
            <input type="text" id="username" name="username" placeholder="Ingresa tu nuevo usuario" required>
        </div>
        <div class="form-section">
            <label for="password">Contraseña</label>
            <input type="password" id="password" name="password" placeholder="Ingresa tu nueva contraseña" required>
        </div>
        <div class="form-buttons">
            <button type="submit">Guardar Cambios</button>
            <button type="button" onclick="window.location.href='perfil.html'">Cancelar</button>
        </div>
    </form>
</main>

<%- include('partials/footer') %>
<script>
    document.addEventListener('DOMContentLoaded', () => {
        fetch('/users/session', { method: 'GET', credentials: 'same-origin' })
            .then(response => response.json())
            .then(data => {
                if (!data.loggedIn) {
                    window.location.href = 'login.html';
                }
            })
            .catch(err => console.error('Error verificando la sesión:', err));
    });
</script>
</body>
</html>
